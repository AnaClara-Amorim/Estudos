function soma(n1=6, n2=8) {
    return n1 + n2
}

//let res = soma(2, 4) pode escrever direto do jeito abaixo. Se enviar somente um valor nesse caso, dá NaN. Se os valores forem definidos antes, como é o caso, ele vai somar o que tiver
console.log(soma(4)) 